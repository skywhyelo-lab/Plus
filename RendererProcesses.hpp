#pragma once

// Вкладка "Процессы" — полная таблица запущенных процессов с фильтром и статус-бейджами.
void RenderTaskManagerTab();

// Вкладка "Исключения" — системный (readonly) и пользовательский (редактируемый) белые списки.
void RenderWhitelistTab();
